{% extends 'base.html.twig' %}

{% block body %}
<h1>{% if question.id %}Modifier{% else %}Ajouter{% endif %} une question</h1>

{{ form_start(form) }}
    {{ form_widget(form) }}
    <button type="submit" class="btn btn-primary">
        {% if question.id %}Modifier{% else %}Ajouter{% endif %} la question
    </button>
{{ form_end(form) }}

<a href="{{ path('quiz_question_index', { course_id: quiz.id, quiz_id: question.getQuiz().id }) }}" class="btn btn-secondary">Retour à la liste des questions</a>
{% endblock %}











